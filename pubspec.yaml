name: proyecto_app_moviles
description: A new Flutter project.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  percent_indicator: ^3.4.0
  firebase_core:
  firebase_auth:
  flutter_bloc:
  equatable:
  google_sign_in:
  cloud_firestore:
  flutter_shimmer:
  flutterfire_ui:
  firebase_storage:
  image_picker: ^0.8.4+11

  geolocator:
  geocoding:
  flutter_polyline_points: ^0.2.6
  google_maps_flutter: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - assets/images/loginBackground.jpg
    - assets/images/spider-plant.png
    - assets/images/plant-pot.png
  fonts:
    - family: Iconos
      fonts:
        - asset: assets/fonts/Iconos.ttf